{
  "name": "cashalot",
  "version": "1.0.0",
  "private": true,
  "description": "Personal dashboard for tracking income, expenses, and savings.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "prettier": "^3.9.6",
    "vitest": "^4.1.10"
  }
}
